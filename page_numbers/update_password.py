import streamlit as st

st.subheader("Password reset")
st.info("Password reset by email is unavailable in local-only mode. Ask the account owner to create a new local account if the password is forgotten.")
